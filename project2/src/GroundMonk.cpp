#include "GroundMonk.h" 

GroundMonk::GroundMonk(const string _path, int _frames, int _speed, int xpos, int ypos, string name, int level, int health, int stamina)
{
}

void GroundMonk::UpperCut(const string _path, int _frames, int _speed) 
{
    SetPath(_path);
    SetFrames(_frames);
    SetSpeed(_speed);
}
void GroundMonk::RockSpike(const string _path, int _frames, int _speed)
{
    SetPath(_path);
    SetFrames(_frames);
    SetSpeed(_speed);
}
void GroundMonk::RockCrush(const string _path, int _frames, int _speed)
{
    SetPath(_path);
    SetFrames(_frames);
    SetSpeed(_speed);
}
void GroundMonk::Meditate(const string _path, int _frames, int _speed)
{
    SetPath(_path);
    SetFrames(_frames);
    SetSpeed(_speed);
}
